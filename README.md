# Retail Customer Insights – Question 2

This project implements Question 2 of your assignment using:

- Customer clustering (k-Means + DBSCAN)
- Deep embedding clustering (autoencoder)
- Association rule mining (Apriori)

Open the notebook in `notebooks/` and run it top-to-bottom after placing the Kaggle CSV in `data/`.
